export default interface Hints {
    optimize?: boolean;
    extract?: boolean;
    notify?: boolean;
    lint?: boolean;
    fix?: boolean;
    map?: boolean;
    cache?: boolean;
    hash?: boolean;
}

export default interface Options {
    root?: string;
    optimize?: boolean;
    extract?: boolean;
    notify?: boolean;
    watch?: boolean;
    lint?: boolean;
    debug?: boolean;
    cache?: boolean;
}
